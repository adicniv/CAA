<?php
class Multibyte_String{
    /**
     *Check if the string is valid for the specified encoding
     *检查字符串在指定的编码里是否有效
     * @param unknown $param
     * @return string
     */
    function mb_check_encoding ($param) {
        ;
        return "";
    }
    /**
     *Get a specific character
     *Get a specific character
     * @param unknown $param
     * @return string
     */
    function mb_chr ($param) {
        ;
        return "";
    }
    /**
     *Perform case folding on a string
     *对字符串进行大小写转换
     * @param unknown $param
     * @return string
     */
    function mb_convert_case ($param) {
        ;
        return "";
    }
    /**
     *Convert character encoding
     *转换字符的编码
     * @param unknown $param
     * @return string
     */
    function mb_convert_encoding($param) {
        ;
        return "";
    }
    /**
     *Convert "kana" one from another ("zen-kaku", "han-kaku" and more)
     *Convert "kana" one from another ("zen-kaku", "han-kaku" and more)
     * @param unknown $param
     * @return string
     */
    function mb_convert_kana ($param) {
        ;
        return "";
    }
    /**
     *Convert character code in variable(s)
     * 转换一个或多个变量的字符编码
     * @param unknown $param
     * @return string
     */
    function fmb_convert_variables ($param) {
        ;
        return "";
    }
    /**
     *Decode string in MIME header field
     * 解码 MIME 头字段中的字符串
     * @param unknown $param
     * @return string
     */
    function mb_decode_mimeheader($param) {
        ;
        return "";
    }
    /**
     *Decode HTML numeric string reference to character
     *根据 HTML 数字字符串解码成字符
     * @param unknown $param
     * @return string
     */
    function mb_decode_numericentity($param) {
        ;
        return "";
    }
    /**
     * Detect character encoding
     * 检测字符的编码
     * @param unknown $param
     * @return string
     */
    function mb_detect_encoding($param) {
        ;
        return "";
    }
    /**
     *Set/Get character encoding detection order
     *设置/获取 字符编码的检测顺序
     * @param unknown $param
     * @return string
     */
    function mb_detect_order ($param) {
        ;
        return "";
    }
    /**
     * Encode string for MIME header
     *   为 MIME 头编码字符串
     * @param unknown $param
     * @return string
     */
    function mb_encode_mimeheader ($param) {
        ;
        return "";
    }
    /**
     *Encode character to HTML numeric string reference
     *Encode character to HTML numeric string reference
     * @param unknown $param
     * @return string
     */
    function mb_encode_numericentity ($param) {
        ;
        return "";
    }
    /**
     * Get aliases of a known encoding type
     * Get aliases of a known encoding type
     * @param unknown $param
     * @return string
     */
    function mb_encoding_aliases ($param) {
        ;
        return "";
    }
    /**
     *Regular expression match for multibyte string
     *
     * @param unknown $param
     * @return string
     */
    function mb_ereg_match($param) {
        ;
        return "";
    }
    /**
     *Perform a regular expression search and replace with multibyte support using a callback
     *
     * @param unknown $param
     * @return string
     */
    function mb_ereg_replace_callback ($param) {
        ;
        return "";
    }
    /**
     *Replace regular expression with multibyte support
     *
     * @param unknown $param
     * @return string
     */
    function mb_ereg_replace ($param) {
        ;
        return "";
    }
    /**
     * Returns start point for next regular expression match
     * 
     * @param unknown $param
     * @return string
     */
    function mb_ereg_search_getpos ($param) {
        ;
        return "";
    }
    /**
     *Retrieve the result from the last multibyte regular expression match
     *
     * @param unknown $param
     * @return string
     */
    function mb_ereg_search_getregs ($param) {
        ;
        return "";
    }
    /**
     *Setup string and regular expression for a multibyte regular expression match
     *
     * @param unknown $param
     * @return string
     */
    function mb_ereg_search_init ($param) {
        ;
        return "";
    }
    /**
     *Returns position and length of a matched part of the multibyte regular expression for a predefined multibyte string
     *
     * @param unknown $param
     * @return string
     */
    function mb_ereg_search_pos ($param) {
        ;
        return "";
    }
    /**
     *Returns the matched part of a multibyte regular expression
     * @param unknown $param
     * @return string
     */
    function mb_ereg_search_regs ($param) {
        ;
        return "";
    }
    /**
     * Set start point of next regular expression match
     * @param unknown $param
     * @return string
     */
    function mb_ereg_search_setpos ($param) {
        ;
        return "";
    }
    /**
     *Multibyte regular expression match for predefined multibyte string
     * @param unknown $param
     * @return string
     */
    function mb_ereg_search ($param) {
        ;
        return "";
    }
    /**
     *Regular expression match with multibyte support
     * @param unknown $param
     * @return string
     */
    function mb_ereg ($param) {
        ;
        return "";
    }
    /**
     *Replace regular expression with multibyte support ignoring case
     * @param unknown $param
     * @return string
     */
    function mb_eregi_replace ($param) {
        ;
        return "";
    }
    /**
     *Regular expression match ignoring case with multibyte support
     * @param unknown $param
     * @return string
     */
    function mb_eregi ($param) {
        ;
        return "";
    }
    /**
     *Get internal settings of mbstring
     * 获取 mbstring 的内部设置
     * @param unknown $param
     * @return string
     */
    function mb_get_info ($param) {
        ;
        return "";
    }
    /**
     * Detect HTTP input character encoding
     * 检测 HTTP 输入字符编码
     * @param unknown $param
     * @return string
     */
    function mb_http_input($param) {
        ;
        return "";
    }
    /**
     *Set/Get HTTP output character encoding
     *设置/获取 HTTP 输出字符编码
     * @param unknown $param
     * @return string
     */
    function mb_http_output ($param) {
        ;
        return "";
    }
    /**
     *Set/Get internal character encoding
     *设置/获取内部字符编码
     * @param unknown $param
     * @return string
     */
    function mb_internal_encoding($param) {
        ;
        return "";
    }
    /**
     *Set/Get current language
     *设置/获取当前的语言
     * @param unknown $param
     * @return string
     */
    function mb_language ($param) {
        ;
        return "";
    }
    /**
     *Returns an array of all supported encodings
     *返回所有支持编码的数组
     * @param unknown $param
     * @return string
     */
    function mb_list_encodings ($param) {
        ;
        return "";
    }
    /**
     * Get code point of character
     * Get code point of character
     * @param unknown $param
     * @return string
     */
    function mb_ord($param) {
        ;
        return "";
    }
    /**
     * Callback function converts character encoding in output buffer
     * 在输出缓冲中转换字符编码的回调函数
     * @param unknown $param
     * @return string
     */
    function mb_output_handler ($param) {
        ;
        return "";
    }
    /**
     *Parse GET/POST/COOKIE data and set global variable
     *解析 GET/POST/COOKIE 数据并设置全局变量
     * @param unknown $param
     * @return string
     */
    function mb_parse_str ($param) {
        ;
        return "";
    }
    /**
     * Get MIME charset string
     * 获取 MIME 字符串
     * @param unknown $param
     * @return string
     */
    function mb_preferred_mime_name ($param) {
        ;
        return "";
    }
    /**
     *Set/Get character encoding for multibyte regex
     *Set/Get character encoding for multibyte regex
     * @param unknown $param
     * @return string
     */
    function mb_regex_encoding ($param) {
        ;
        return "";
    }
    /**
     *Set/Get the default options for mbregex functions
     *Set/Get the default options for mbregex functions
     * @param unknown $param
     * @return string
     */
    function fmb_regex_set_options ($param) {
        ;
        return "";
    }
    /**
     *Description
     *Description
     * @param unknown $param
     * @return string
     */
    function mb_scrub ($param) {
        ;
        return "";
    }
    /**
     *Send encoded mail
     *发送编码过的邮件
     * @param unknown $param
     * @return string
     */
    function mb_send_mail ($param) {
        ;
        return "";
    }
    /**
     * Split multibyte string using regular expression
     * 使用正则表达式分割多字节字符串
     * @param unknown $param
     * @return string
     */
    function mb_split ($param) {
        ;
        return "";
    }
    /**
     *Get part of string
     * 获取字符的一部分
     * @param unknown $param
     * @return string
     */
    function mb_strcut ($param) {
        ;
        return "";
    }
    /**
     *Get truncated string with specified width
     *获取按指定宽度截断的字符串
     * @param unknown $param
     * @return string
     */
    function mb_strimwidth ($param) {
        ;
        return "";
    }
    /**
     * Finds position of first occurrence of a string within another, case insensitive
     * 大小写不敏感地查找字符串在另一个字符串中首次出现的位置
     * @param unknown $param
     * @return string
     */
    function mb_stripos ($param) {
        ;
        return "";
    }
    /**
     *Finds first occurrence of a string within another, case insensitive
     *大小写不敏感地查找字符串在另一个字符串里的首次出现
     * @param unknown $param
     * @return string
     */
    function mb_stristr($param) {
        ;
        return "";
    }
    /**
     *Get string length
     *获取字符串的长度
     * @param unknown $param
     * @return string
     */
    function mb_strlen($param) {
        ;
        return "";
    }
    /**
     *Find position of first occurrence of string in a string
     *查找字符串在另一个字符串中首次出现的位置
     * @param unknown $param
     * @return string
     */
    function mb_strpos($param) {
        ;
        return "";
    }
    /**
     *Finds the last occurrence of a character in a string within another
     *查找指定字符在另一个字符串中最后一次的出现
     * @param unknown $param
     * @return string
     */
    function mb_strrchr ($param) {
        ;
        return "";
    }
    /**
     *Finds the last occurrence of a character in a string within another, case insensitive
     *大小写不敏感地查找指定字符在另一个字符串中最后一次的出现
     * @param unknown $param
     * @return string
     */
    function mb_strrichr($param) {
        ;
        return "";
    }
    /**
     *Finds position of last occurrence of a string within another, case insensitive
     *大小写不敏感地在字符串中查找一个字符串最后出现的位置
     * @param unknown $param
     * @return string
     */
    function mb_strripos($param) {
        ;
        return "";
    }
    /**
     *Find position of last occurrence of a string in a string
     *查找字符串在一个字符串中最后出现的位置
     * @param unknown $param
     * @return string
     */
    function mb_strrpos($param) {
        ;
        return "";
    }
    /**
     *Finds first occurrence of a string within another
     *查找字符串在另一个字符串里的首次出现
     * @param unknown $param
     * @return string
     */
    function mb_strstr ($param) {
        ;
        return "";
    }
    /**
     *Make a string lowercase
     *使字符串小写
     * @param unknown $param
     * @return string
     */
    function mb_strtolower ($param) {
        ;
        return "";
    }
    /**
     *Make a string uppercase
     *使字符串大写
     * @param unknown $param
     * @return string
     */
    function mb_strtoupper ($param) {
        ;
        return "";
    }
    /**
     *
     *Return width of string
     * 返回字符串的宽度
     * @param unknown $param
     * @return string
     */
    function mb_strwidth ($param) {
        ;
        return "";
    }
    /**
     *Set/Get substitution character
     *设置/获取替代字符
     * @param unknown $param
     * @return string
     */
    function mb_substitute_character ($param) {
        ;
        return "";
    }
    /**
     *Count the number of substring occurrences
     *统计字符串出现的次数
     * @param unknown $param
     * @return string
     */
    function mb_substr_count ($param) {
        ;
        return "";
    }
    /**
     *Get part of string
     *获取部分字符串
     * @param unknown $param
     * @return string
     */
    function mb_substr ($param) {
        ;
        return "";
    }
}