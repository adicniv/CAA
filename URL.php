<?php
class URL{
    /**
     * — 对使用 MIME base64 编码的数据进行解码
     * @param unknown $param
     * @return string
     */
    function base64_decode($param) {
        ;
        return "";
    }
    /**
     * — 使用 MIME base64 对数据进行编码
     * @param unknown $param
     * @return string
     */
    function base64_encode($param) {
        ;
        return "";
    }
    /**
     * — 取得服务器响应一个 HTTP 请求所发送的所有标头
     * @param unknown $param
     * @return string
     */
    function get_headers($param) {
        ;
        return "";
    }
    /**
     * — 从一个文件中提取所有的 meta 标签 content 属性，返回一个数组
     * @param unknown $param
     * @return string
     */
    function get_meta_tags($param) {
        ;
        return "";
    }
    /**
     * — 生成 URL-encode 之后的请求字符串
     * @param unknown $param
     * @return string
     */
    function http_build_query($param) {
        ;
        return "";
    }
    /**
     * — 解析 URL，返回其组成部分
     * @param unknown $param
     * @return string
     */
    function parse_url($param) {
        ;
        return "";
    }
    /**
     * — 对已编码的 URL 字符串进行解码
     * @param unknown $param
     * @return string
     */
    function rawurldecode($param) {
        ;
        return "";
    }
    /**
     * — 按照 RFC 3986 对 URL 进行编码
     * @param unknown $param
     * @return string
     */
    function rawurlencode($param) {
        ;
        return "";
    }
    /**
     * — 解码已编码的 URL 字符串
     * @param unknown $param
     * @return string
     */
    function urldecode($param) {
        ;
        return "";
    }
    /**
     * — 编码 URL 字符串
     * @param unknown $param
     * @return string
     */
    function urlencode($param) {
        ;
        return "";
    }
}