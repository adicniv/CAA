<?php
class SeasLog{
    /**
     *SeasLog::alert — 记录 alert 日志
     * @param unknown $param
     * @return string
     */
    function alert($param) {
        SeasLog::alert;
        return "";
    }
    /**
     *SeasLog::analyzerCount — Get log count by level, log_path and key_word
     * @param unknown $param
     * @return string
     */
    function analyzerCount($param) {
        SeasLog::analyzerCount;
        return "";
    }
    /**
     *SeasLog::analyzerDetail — Get log detail by level, log_path, key_word, start, limit, order
     * @param unknown $param
     * @return string
     */
    function analyzerDetail($param) {
        ;
        return "";
    }
    /**
     *SeasLog::closeLoggerStream — Manually release stream flow from logger
     * @param unknown $param
     * @return string
     */
    function closeLoggerStream($param) {
        ;
        return "";
    }
    /**
     *SeasLog::__construct — Description
     * @param unknown $param
     * @return string
     */
    function __construct($param) {
        ;
        return "";
    }
    /**
     *SeasLog::critical — 记录 critical 日志
     * @param unknown $param
     * @return string
     */
    function critical($param) {
        ;
        return "";
    }
    /**
     *SeasLog::debug — 记录 debug 日志
     * @param unknown $param
     * @return string
     */
    function debug($param) {
        ;
        return "";
    }
    /**
     *SeasLog::__destruct — Description
     * @param unknown $param
     * @return string
     */
    function __destruct($param) {
        ;
        return "";
    }
    /**
     *SeasLog::emergency — 记录 emergency 日志
     * @param unknown $param
     * @return string
     */
    function emergency($param) {
        ;
        return "";
    }
    /**
     *SeasLog::error — 记录 error 日志
     * @param unknown $param
     * @return string
     */
    function error($param) {
        ;
        return "";
    }
    /**
     *SeasLog::flushBuffer — 将日志缓存刷新到介质中，文件介质，或者发送到远端的 TCP/UDP 服务地址。
     * @param unknown $param
     * @return string
     */
    function flushBuffer($param) {
        ;
        return "";
    }
    /**
     *SeasLog::getBasePath — 获得 SeasLog 根目录
     * @param unknown $param
     * @return string
     */
    function getBasePath($param) {
        ;
        return "";
    }
    /**
     *SeasLog::getBuffer — 获取内存中的日志缓存数组
     * @param unknown $param
     * @return string
     */
    function getBuffer($param) {
        ;
        return "";
    }
    /**
     *SeasLog::getBufferEnabled — Determin if buffer enabled
     * @param unknown $param
     * @return string
     */
    function getBufferEnabled($param) {
        ;
        return "";
    }
    /**
     *SeasLog::getDatetimeFormat — 获取 SeasLog 日期格式
     * @param unknown $param
     * @return string
     */
    function getDatetimeFormat($param) {
        ;
        return "";
    }
    /**
     *SeasLog::getLastLogger — 获得 SeasLog 最近的一次 Logger 名称
     * @param unknown $param
     * @return string
     */
    function getLastLogger($param) {
        ;
        return "";
    }
    /**
     *SeasLog::getRequestID — 获得当前 SeasLog 中用于区分请求的 request_id
     * @param unknown $param
     * @return string
     */
    function getRequestID($param) {
        ;
        return "";
    }
    /**
     *SeasLog::getRequestVariable — Get SeasLog request variable
     * @param unknown $param
     * @return string
     */
    function getRequestVariable($param) {
        ;
        return "";
    }
    /**
     *SeasLog::info — Record info log information
     * @param unknown $param
     * @return string
     */
    function info($param) {
        ;
        return "";
    }
    /**
     *SeasLog::log — 公共的日志记录函数
     * @param unknown $param
     * @return string
     */
    function log($param) {
        ;
        return "";
    }
    /**
     *SeasLog::notice — 记录 notice 日志
     * @param unknown $param
     * @return string
     */
    function notice($param) {
        ;
        return "";
    }
    /**
     *SeasLog::setBasePath — 设置 SeasLog 根目录
     * @param unknown $param
     * @return string
     */
    function setBasePath($param) {
        ;
        return "";
    }
    /**
     *SeasLog::setDatetimeFormat — 设置 SeasLog 日期格式
     * @param unknown $param
     * @return string
     */
    function setDatetimeFormat($param) {
        ;
        return "";
    }
    /**
     *SeasLog::setLogger — 设置 SeasLog 的 Logger 名
     * @param unknown $param
     * @return string
     */
    function setLogger($param) {
        ;
        return "";
    }
    /**
     *SeasLog::setRequestID — 设置可以由 SeasLog 用于区分请求的 request_id
     * @param unknown $param
     * @return string
     */
    function setRequestID($param) {
        ;
        return "";
    }
    /**
     *SeasLog::setRequestVariable — Manually set SeasLog request variable
     * @param unknown $param
     * @return string
     */
    function setRequestVariable($param) {
        ;
        return "";
    }
    /**
     *SeasLog::warning — Record warning log information
     * @param unknown $param
     * @return string
     */
    function warning($param) {
        ;
        return "";
    }
}
seaslog_get_author(); //— 获取 SeasLog 作者
seaslog_get_version(); //— 获取 SeasLog 的版本