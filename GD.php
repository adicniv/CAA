<?php
class {
    /**
     *取得当前安装的 GD 库的信息
     *Retrieve information about the currently installed GD library
     * @param unknown $param
     * @return string
     */
    function gd_info($param) {
        ;
        return "";
    }
    /**
     * 取得图像大小
     * Get the size of an image
     * @param unknown $param
     * @return string
     */
    function getimagesize ($param) {
        ;
        return "";
    }
    /**
     *从字符串中获取图像尺寸信息
     *Get the size of an image from a string
     * @param unknown $param
     * @return string
     */
    function getimagesizefromstring ($param) {
        ;
        return "";
    }
    /**
     * 取得图像类型的文件后缀
     * Get file extension for image type
     * @param unknown $param
     * @return string
     */
    function image_type_to_extension($param) {
        ;
        return "";
    }
    /**
     *取得 getimagesize，exif_read_data，exif_thumbnail，exif_imagetype 所返回的图像类型的 MIME 类型
     *Get Mime-Type for image-type returned by getimagesize, exif_read_data, exif_thumbnail, exif_imagetype
     * @param unknown $param
     * @return string
     */
    function image_type_to_mime_type ($param) {
        ;
        return "";
    }
    /**
     * 以 WBMP 格式将图像输出到浏览器或文件
     * Output image to browser or file
     * @param unknown $param
     * @return string
     */
    function image2wbmp ($param) {
        ;
        return "";
    }
    /**
     * 返回经过仿射变换后的图像，剪切区域可选
     *  Return an image containing the affine transformed src image, using an optional clipping area
     * @param unknown $param
     * @return string
     */
    function imageaffine ($param) {
        ;
        return "";
    }
    /**
     *Concatenate two affine transformation matrices
     *Concatenate two affine transformation matrices
     * @param unknown $param
     * @return string
     */
    function imageaffinematrixconcat($param) {
        ;
        return "";
    }
    /**
     *Get an affine transformation matrix
     *Get an affine transformation matrix
     * @param unknown $param
     * @return string
     */
    function imageaffinematrixget ($param) {
        ;
        return "";
    }
    /**
     *设定图像的混色模式
     *Set the blending mode for an image
     * @param unknown $param
     * @return string
     */
    function imagealphablending ($param) {
        ;
        return "";
    }
    /**
     *是否使用抗锯齿（antialias）功能
     * Should antialias functions be used or not
     * @param unknown $param
     * @return string
     */
    function imageantialias ($param) {
        ;
        return "";
    }
    /**
     *画椭圆弧
     *Draws an arc
     * @param unknown $param
     * @return string
     */
    function imagearc($param) {
        ;
        return "";
    }
    /**
     *Output a BMP image to browser or file
     *Output a BMP image to browser or file
     * @param unknown $param
     * @return string
     */
    function imagebmp ($param) {
        ;
        return "";
    }
    /**
     *水平地画一个字符
     *Draw a character horizontally
     * @param unknown $param
     * @return string
     */
    function imagechar ($param) {
        ;
        return "";
    }
    /**
     *垂直地画一个字符
     *Draw a character vertically
     * @param unknown $param
     * @return string
     */
    function imagecharup ($param) {
        ;
        return "";
    }
    /**
     *为一幅图像分配颜色
     *Allocate a color for an image
     * @param unknown $param
     * @return string
     */
    function imagecolorallocate ($param) {
        ;
        return "";
    }
    /**
     *为一幅图像分配颜色 + alpha
     *Allocate a color for an image
     * @param unknown $param
     * @return string
     */
    function imagecolorallocatealpha($param) {
        ;
        return "";
    }
    /**
     *取得某像素的颜色索引值
     *Get the index of the color of a pixel
     * @param unknown $param
     * @return string
     */
    function imagecolorat($param) {
        ;
        return "";
    }
    /**
     * 取得与指定的颜色最接近的颜色的索引值
     * Get the index of the closest color to the specified color
     * @param unknown $param
     * @return string
     */
    function imagecolorclosest ($param) {
        ;
        return "";
    }
    /**
     *取得与指定的颜色加透明度最接近的颜色
     *Get the index of the closest color to the specified color + alpha
     * @param unknown $param
     * @return string
     */
    function imagecolorclosestalpha     ($param) {
        ;
        return "";
    }
    /**
     * 取得与给定颜色最接近的色度的黑白色的索引
     * Get the index of the color which has the hue, white and blackness
     * @param unknown $param
     * @return string
     */
    function imagecolorclosesthwb($param) {
        ;
        return "";
    }
    /**
     *取消图像颜色的分配
     * De-allocate a color for an image
     * @param unknown $param
     * @return string
     */
    function imagecolordeallocate($param) {
        ;
        return "";
    }
    /**
     *取得指定颜色的索引值
     *Get the index of the specified color
     * @param unknown $param
     * @return string
     */
    function imagecolorexact ($param) {
        ;
        return "";
    }
    /**
     *取得指定的颜色加透明度的索引值
     *Get the index of the specified color + alpha
     * @param unknown $param
     * @return string
     */
    function imagecolorexactalpha ($param) {
        ;
        return "";
    }
    /**
     *使一个图像中调色板版本的颜色与真彩色版本更能匹配
     *Makes the colors of the palette version of an image more closely match the true color version
     * @param unknown $param
     * @return string
     */
    function imagecolormatch ($param) {
        ;
        return "";
    }
    /**
     *取得指定颜色的索引值或有可能得到的最接近的替代值
     *Get the index of the specified color or its closest possible alternative
     * @param unknown $param
     * @return string
     */
    function imagecolorresolve ($param) {
        ;
        return "";
    }
    /**
     *取得指定颜色 + alpha 的索引值或有可能得到的最接近的替代值
     * Get the index of the specified color + alpha or its closest possible alternative
     * @param unknown $param
     * @return string
     */
    function imagecolorresolvealpha ($param) {
        ;
        return "";
    }
    /**
     *给指定调色板索引设定颜色
     *Set the color for the specified palette index
     * @param unknown $param
     * @return string
     */
    function imagecolorset ($param) {
        ;
        return "";
    }
    /**
     *取得某索引的颜色
     *Get the colors for an index
     * @param unknown $param
     * @return string
     */
    function imagecolorsforindex ($param) {
        ;
        return "";
    }
    /**
     *取得一幅图像的调色板中颜色的数目
     *Find out the number of colors in an image's palette
     * @param unknown $param
     * @return string
     */
    function imagecolorstotal($param) {
        ;
        return "";
    }
    /**
     *将某个颜色定义为透明色
     * Define a color as transparent
     * @param unknown $param
     * @return string
     */
    function fimagecolortransparent ($param) {
        ;
        return "";
    }
    /**
     *用系数 div 和 offset 申请一个 3x3 的卷积矩阵
     *Apply a 3x3 convolution matrix, using coefficient and offset
     * @param unknown $param
     * @return string
     */
    function imageconvolution($param) {
        ;
        return "";
    }
    /**
     * 拷贝图像的一部分
     * Copy part of an image    
     * @param unknown $param
     * @return string
     */
    function imagecopy ($param) {
        ;
        return "";
    }
    /**
     *拷贝并合并图像的一部分
     *Copy and merge part of an image
     * @param unknown $param
     * @return string
     */
    function imagecopymerge ($param) {
        ;
        return "";
    }
    /**
     *用灰度拷贝并合并图像的一部分
     *Copy and merge part of an image with gray scale
     * @param unknown $param
     * @return string
     */
    function imagecopymergegray($param) {
        ;
        return "";
    }
    /**
     *重采样拷贝部分图像并调整大小
     *Copy and resize part of an image with resampling
     * @param unknown $param
     * @return string
     */
    function imagecopyresampled ($param) {
        ;
        return "";
    }
    /**
     *拷贝部分图像并调整大小
     *Copy and resize part of an image
     * @param unknown $param
     * @return string
     */
    function imagecopyresized ($param) {
        ;
        return "";
    }
    /**
     *新建一个基于调色板的图像
     *Create a new palette based image
     * @param unknown $param
     * @return string
     */
    function imagecreate($param) {
        ;
        return "";
    }
    /**
     *由文件或 URL 创建一个新图象。
     *Create a new image from file or URL
     * @param unknown $param
     * @return string
     */
    function imagecreatefrombmp ($param) {
        ;
        return "";
    }
    /**
     *从 GD2 文件或 URL 新建一图像
     *Create a new image from GD2 file or URL
     * @param unknown $param
     * @return string
     */
    function imagecreatefromgd2 ($param) {
        ;
        return "";
    }
    /**
     * 从给定的 GD2 文件或 URL 中的部分新建一图像
     * Create a new image from a given part of GD2 file or URL
     * @param unknown $param
     * @return string
     */
    function imagecreatefromgd2part ($param) {
        ;
        return "";
    }
    /**
     *从 GD 文件或 URL 新建一图像
     *Create a new image from GD file or URL
     * @param unknown $param
     * @return string
     */
    function imagecreatefromgd ($param) {
        ;
        return "";
    }
    /**
     *由文件或 URL 创建一个新图象。
     *Create a new image from file or URL
     * @param unknown $param
     * @return string
     */
    function imagecreatefromgif ($param) {
        ;
        return "";
    }
    /**
     *由文件或 URL 创建一个新图象。
     *Create a new image from file or URL
     * @param unknown $param
     * @return string
     */
    function imagecreatefromjpeg($param) {
        ;
        return "";
    }
    /**
     *由文件或 URL 创建一个新图象。
     *Create a new image from file or URL
     * @param unknown $param
     * @return string
     */
    function imagecreatefrompng ($param) {
        ;
        return "";
    }
    /**
     *从字符串中的图像流新建一图像
     *Create a new image from the image stream in the string
     * @param unknown $param
     * @return string
     */
    function imagecreatefromstring ($param) {
        ;
        return "";
    }
    /**
     *由文件或 URL 创建一个新图象。
     *Create a new image from file or URL
     * @param unknown $param
     * @return string
     */
    function imagecreatefromwbmp ($param) {
        ;
        return "";
    }
    /**
     * 由文件或 URL 创建一个新图象。
     * Create a new image from file or URL
     * @param unknown $param
     * @return string
     */
    function imagecreatefromwebp ($param) {
        ;
        return "";
    }
    /**
     * 由文件或 URL 创建一个新图象。
     * Create a new image from file or URL
     * @param unknown $param
     * @return string
     */
    function imagecreatefromxbm ($param) {
        ;
        return "";
    }
    /**
     *由文件或 URL 创建一个新图象。
     *Create a new image from file or URL
     * @param unknown $param
     * @return string
     */
    function imagecreatefromxpm($param) {
        ;
        return "";
    }
    /**
     *新建一个真彩色图像
     * Create a new true color image
     * @param unknown $param
     * @return string
     */
    function imagecreatetruecolor ($param) {
        ;
        return "";
    }
    /**
     * Crop an image to the given rectangle
     *  Crop an image to the given rectangle
     * @param unknown $param
     * @return string
     */
    function imagecrop ($param) {
        ;
        return "";
    }
    /**
     *Crop an image automatically using one of the available modes
     *Crop an image automatically using one of the available modes
     * @param unknown $param
     * @return string
     */
    function imagecropauto ($param) {
        ;
        return "";
    }
    /**
     *画一虚线
     *Draw a dashed line
     * @param unknown $param
     * @return string
     */
    function imagedashedline $param) {
        ;
        return "";
    }
    /**
     *销毁一图像
     * Destroy an image
     * @param unknown $param
     * @return string
     */
    function imagedestroy ($param) {
        ;
        return "";
    }
    /**
     *画一个椭圆
     *Draw an ellipse
     * @param unknown $param
     * @return string
     */
    function imageellipse ($param) {
        ;
        return "";
    }
    /**
     *区域填充
     *Flood fill
     * @param unknown $param
     * @return string
     */
    function imagefill ($param) {
        ;
        return "";
    }
    /**
     *画一椭圆弧且填充
     *Draw a partial arc and fill it
     * @param unknown $param
     * @return string
     */
    function imagefilledarc($param) {
        ;
        return "";
    }
    /**
     *画一椭圆并填充
     *Draw a filled ellipse
     * @param unknown $param
     * @return string
     */
    function imagefilledellipse($param) {
        ;
        return "";
    }
    /**
     *画一多边形并填充
     *Draw a filled polygon
     * @param unknown $param
     * @return string
     */
    function imagefilledpolygon ($param) {
        ;
        return "";
    }
    /**
     *画一矩形并填充
     *Draw a filled rectangle
     * @param unknown $param
     * @return string
     */
    function imagefilledrectangle ($param) {
        ;
        return "";
    }
    /**
     * 区域填充到指定颜色的边界为止
     * Flood fill to specific color
     * @param unknown $param
     * @return string
     */
    function imagefilltoborder($param) {
        ;
        return "";
    }
    /**
     *对图像使用过滤器
     * Applies a filter to an image
     * @param unknown $param
     * @return string
     */
    function imagefilter($param) {
        ;
        return "";
    }
    /**
     *Flips an image using a given mode
     *Flips an image using a given mode
     * @param unknown $param
     * @return string
     */
    function imageflip ($param) {
        ;
        return "";
    }
    /**
     * 取得字体高度
     * Get font height
     * @param unknown $param
     * @return string
     */
    function imagefontheight ($param) {
        ;
        return "";
    }
    /**
     *取得字体宽度
     *Get font width
     * @param unknown $param
     * @return string
     */
    function imagefontwidth ($param) {
        ;
        return "";
    }
    /**
     *给出一个使用 FreeType 2 字体的文本框
     *Give the bounding box of a text using fonts via freetype2
     * @param unknown $param
     * @return string
     */
    function imageftbbox ($param) {
        ;
        return "";
    }
    /**
     *使用 FreeType 2 字体将文本写入图像
     * Write text to the image using fonts using FreeType 2
     * @param unknown $param
     * @return string
     */
    function imagefttext ($param) {
        ;
        return "";
    }
    /**
     * 对 GD 图像应用 gamma 修正
     * Apply a gamma correction to a GD image
     * @param unknown $param
     * @return string
     */
    function imagegammacorrect($param) {
        ;
        return "";
    }
    /**
     * 将 GD2 图像输出到浏览器或文件
     *  Output GD2 image to browser or file
     * @param unknown $param
     * @return string
     */
    function imagegd2   ($param) {
        ;
        return "";
    }
    /**
     * 将 GD 图像输出到浏览器或文件
     * Output GD image to browser or file
     * @param unknown $param
     * @return string
     */
    function imagegd ($param) {
        ;
        return "";
    }
    /**
     * Get the clipping rectangle
     * Get the clipping rectangle
     * @param unknown $param
     * @return string
     */
    function imagegetclip ($param) {
        ;
        return "";
    }
    /**
     *输出图象到浏览器或文件。
     *Output image to browser or file
     * @param unknown $param
     * @return string
     */
    function imagegif ($param) {
        ;
        return "";
    }
    /**
     * Captures the whole screen
     * Captures the whole screen
     * @param unknown $param
     * @return string
     */
    function imagegrabscreen($param) {
        ;
        return "";
    }
    /**
     *Captures a window
     *Captures a window
     * @param unknown $param
     * @return string
     */
    function imagegrabwindow ($param) {
        ;
        return "";
    }
    /**
     *激活或禁止隔行扫描
     *Enable or disable interlace
     * @param unknown $param
     * @return string
     */
    function imageinterlace($param) {
        ;
        return "";
    }
    /**
     *检查图像是否为真彩色图像
     * Finds whether an image is a truecolor image
     * @param unknown $param
     * @return string
     */
    function imageistruecolor ($param) {
        ;
        return "";
    }
    /**
     * 输出图象到浏览器或文件。
     * Output image to browser or file
     * @param unknown $param
     * @return string
     */
    function imagejpeg ($param) {
        ;
        return "";
    }
    /**
     *设定 alpha 混色标志以使用绑定的 libgd 分层效果
     *Set the alpha blending flag to use layering effects
     * @param unknown $param
     * @return string
     */
    function imagelayereffect ($param) {
        ;
        return "";
    }
    /**
     *画一条线段
     *Draw a line
     *
     * @param unknown $param
     * @return string
     */
    function imageline($param) {
        ;
        return "";
    }
    /**
     *载入一新字体
     *Load a new font
     *
     * @param unknown $param
     * @return string
     */
    function imageloadfont ($param) {
        ;
        return "";
    }
    /**
     *Draws an open polygon
     *Draws an open polygon
     * @param unknown $param
     * @return string
     */
    function imageopenpolygon  ($param) {
        ;
        return "";
    }
    /**
     *将调色板从一幅图像拷贝到另一幅
     *Copy the palette from one image to another
     * @param unknown $param
     * @return string
     */
    function imagepalettecopy ($param) {
        ;
        return "";
    }
    /**
     *Converts a palette based image to true color
     *Converts a palette based image to true color
     * @param unknown $param
     * @return string
     */
    function imagepalettetotruecolor($param) {
        ;
        return "";
    }
    /**
     * 以 PNG 格式将图像输出到浏览器或文件
     * Output a PNG image to either the browser or a file
     * @param unknown $param
     * @return string
     */
    function imagepng ($param) {
        ;
        return "";
    }
    /**
     *画一个多边形
     *Draws a polygon
     * @param unknown $param
     * @return string
     */
    function imagepolygon ($param) {
        ;
        return "";
    }
    /**
     * 给出一个使用 PostScript Type1 字体的文本方框 
     * Give the bounding box of a text rectangle using PostScript Type1 fonts 
     * @param unknown $param
     * @return string
     */
    function imagepsbbox ($param) {
        ;
        return "";
    }
    /**
     *改变字体中的字符编码矢量
     *Change the character encoding vector of a font
     * @param unknown $param
     * @return string
     */
    function imagepsencodefont ($param) {
        ;
        return "";
    }
    /**
     *扩充或精简字体
     *Extend or condense a font
     * @param unknown $param
     * @return string
     */
    function imagepsextendfont ($param) {
        ;
        return "";
    }
    /**
     *释放一个 PostScript Type 1 字体所占用的内存
     * Free memory used by a PostScript Type 1 font
     * @param unknown $param
     * @return string
     */
    function imagepsfreefont ($param) {
        ;
        return "";
    }
    /**
     * 从文件中加载一个 PostScript Type 1 字体
     * Load a PostScript Type 1 font from file
     * @param unknown $param
     * @return string
     */
    function imagepsloadfont($param) {
        ;
        return "";
    }
    /**
     *倾斜某字体
     *Slant a font
     * @param unknown $param
     * @return string
     */
    function imagepsslantfont($param) {
        ;
        return "";
    }
    /**
     *用 PostScript Type1 字体把文本字符串画在图像上
     *Draws a text over an image using PostScript Type1 fonts
     * @param unknown $param
     * @return string
     */
    function imagepstext ($param) {
        ;
        return "";
    }
    /**
     *画一个矩形
     *Draw a rectangle
     * @param unknown $param
     * @return string
     */
    function imagerectangle($param) {
        ;
        return "";
    }
    /**
     *Get or set the resolution of the image
     *Get or set the resolution of the image
     * @param unknown $param
     * @return string
     */
    function imageresolution($param) {
        ;
        return "";
    }
    /**
     *用给定角度旋转图像
     *Rotate an image with a given angle
     * @param unknown $param
     * @return string
     */
    function imagerotate ($param) {
        ;
        return "";
    }
    /**
     *设置标记以在保存 PNG 图像时保存完整的 alpha 通道信息（与单一透明色相反）
     * Whether to retain full alpha channel information when saving PNG images
     * @param unknown $param
     * @return string
     */
    function imagesavealpha ($param) {
        ;
        return "";
    }
    /**
     *Scale an image using the given new width and height
     *Scale an image using the given new width and height
     * @param unknown $param
     * @return string
     */
    function imagescale($param) {
        ;
        return "";
    }
    /**
     *设定画线用的画笔图像
     *Set the brush image for line drawing
     * @param unknown $param
     * @return string
     */
    function imagesetbrush ($param) {
        ;
        return "";
    }
    /**
     * Set the clipping rectangle
     * Set the clipping rectangle
     * @param unknown $param
     * @return string
     */
    function imagesetclip ($param) {
        ;
        return "";
    }
    /**
     * Set the interpolation method
     * Set the interpolation method
     * @param unknown $param
     * @return string
     */
    function imagesetinterpolation($param) {
        ;
        return "";
    }
    /**
     * 画一个单一像素
     * Set a single pixel
     * @param unknown $param
     * @return string
     */
    function imagesetpixel ($param) {
        ;
        return "";
    }
    /**
     *设定画线的风格
     *Set the style for line drawing
     * @param unknown $param
     * @return string
     */
    function imagesetstyle ($param) {
        ;
        return "";
    }
    /**
     *设定画线的宽度
     *Set the thickness for line drawing
     * @param unknown $param
     * @return string
     */
    function imagesetthickness ($param) {
        ;
        return "";
    }
    /**
     * 设定用于填充的贴图
     * Set the tile image for filling
     * @param unknown $param
     * @return string
     */
    function imagesettile ($param) {
        ;
        return "";
    }
    /**
     *水平地画一行字符串
     *Draw a string horizontally
     * @param unknown $param
     * @return string
     */
    function imagestring ($param) {
        ;
        return "";
    }
    /**
     *垂直地画一行字符串
     *Draw a string vertically
     * @param unknown $param
     * @return string
     */
    function imagestringup ($param) {
        ;
        return "";
    }
    /**
     *取得图像宽度
     *Get image width
     * @param unknown $param
     * @return string
     */
    function imagesx($param) {
        ;
        return "";
    }
    /**
     *取得图像高度
     *Get image height
     * @param unknown $param
     * @return string
     */
    function imagesy($param) {
        ;
        return "";
    }
    /**
     *将真彩色图像转换为调色板图像
     *Convert a true color image to a palette image
     * @param unknown $param
     * @return string
     */
    function imagetruecolortopalette($param) {
        ;
        return "";
    }
    /**
     *取得使用 TrueType 字体的文本的范围
     *Give the bounding box of a text using TrueType fonts
     * @param unknown $param
     * @return string
     */
    function imagettfbbox($param) {
        ;
        return "";
    }
    /**
     *用 TrueType 字体向图像写入文本
     *Write text to the image using TrueType fonts

     * @param unknown $param
     * @return string
     */
    function imagettftext ($param) {
        ;
        return "";
    }
    /**
     * 返回当前 PHP 版本所支持的图像类型
     * Return the image types supported by this PHP build
     * @param unknown $param
     * @return string
     */
    function imagetypes($param) {
        ;
        return "";
    }
    /**
     *以 WBMP 格式将图像输出到浏览器或文件
     *Output image to browser or file
     * @param unknown $param
     * @return string
     */
    function imagewbmp ($param) {
        ;
        return "";
    }
    /**
     *将 WebP 格式的图像输出到浏览器或文件
     *Output a WebP image to browser or file
     * @param unknown $param
     * @return string
     */
    function imagewebp ($param) {
        ;
        return "";
    }
    /**
     *将 XBM 图像输出到浏览器或文件
     *Output an XBM image to browser or file
     * @param unknown $param
     * @return string
     */
    function imagexbm ($param) {
        ;
        return "";
    }
    /**
     *将二进制 IPTC 数据嵌入到一幅 JPEG 图像中
     *Embeds binary IPTC data into a JPEG image
     * @param unknown $param
     * @return string
     */
    function iptcembed ($param) {
        ;
        return "";
    }
    /**
     *将二进制 IPTC 块解析为单个标记
     * Parse a binary IPTC block into single tags
     * @param unknown $param
     * @return string
     */
    function iptcparse($param) {
        ;
        return "";
    }
    /**
     *将 JPEG 图像文件转换为 WBMP 图像文件
     *Convert JPEG image file to WBMP image file
     * @param unknown $param
     * @return string
     */
    function jpeg2wbmp ($param) {
        ;
        return "";
    }
    /**
     *将 PNG 图像文件转换为 WBMP 图像文件
     *Convert PNG image file to WBMP image file
     * @param unknown $param
     * @return string
     */
    function png2wbmp($param) {
        ;
        return "";
    }    
}