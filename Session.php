<?php
class Session {
    /**
     *Discard session array changes and finish session
     *Discard session array changes and finish session
     * @param unknown $param
     * @return string
     */
    function session_abort ($param) {
        ;
        return "";
    }
    /**
     *返回当前缓存的到期时间
     *Return current cache expire
     * @param unknown $param
     * @return string
     */
    function session_cache_expire($param) {
        ;
        return "";
    }
    /**
     *读取/设置缓存限制器
     * Get and/or set the current cache limiter
     * @param unknown $param
     * @return string
     */
    function session_cache_limiter ($param) {
        ;
        return "";
    }
    /**
     * session_write_close 的别名
     * Alias of session_write_close
     * @param unknown $param
     * @return string
     */
    function session_commit($param) {
        ;
        return "";
    }
    /**
     * Create new session id
     *  Create new session id
     * @param unknown $param
     * @return string
     */
    function fsession_create_id ($param) {
        ;
        return "";
    }
    /**
     *解码会话数据
     *Decodes session data from a session encoded string
     * @param unknown $param
     * @return string
     */
    function session_decode ($param) {
        ;
        return "";
    }
    /**
     *销毁一个会话中的全部数据
     *Destroys all data registered to a session
     * @param unknown $param
     * @return string
     */
    function session_destroy ($param) {
        ;
        return "";
    }
    /**
     *将当前会话数据编码为一个字符串
     *Encodes the current session data as a session encoded string
     * @param unknown $param
     * @return string
     */
    function session_encode($param) {
        ;
        return "";
    }
    /**
     *Perform session data garbage collection
     * Perform session data garbage collection
     * @param unknown $param
     * @return string
     */
    function session_gc($param) {
        ;
        return "";
    }
    /**
     * 获取会话 cookie 参数
     * Get the session cookie parameters
     * @param unknown $param
     * @return string
     */
    function session_get_cookie_params ($param) {
        ;
        return "";
    }
    /**
     *获取/设置当前会话 ID
     *Get and/or set the current session id
     * @param unknown $param
     * @return string
     */
    function session_id ($param) {
        ;
        return "";
    }
    /**
     *检查变量是否在会话中已经注册
     * Find out whether a global variable is registered in a session
     * @param unknown $param
     * @return string
     */
    function session_is_registered($param) {
        ;
        return "";
    }
    /**
     * 获取/设置会话模块名称
     * Get and/or set the current session module
     * @param unknown $param
     * @return string
     */
    function fsession_module_name($param) {
        ;
        return "";
    }
    /**
     * 读取/设置会话名称
     * Get and/or set the current session name
     * @param unknown $param
     * @return string
     */
    function session_name($param) {
        ;
        return "";
    }
    /**
     *使用新生成的会话 ID 更新现有会话 ID
     *Update the current session id with a newly generated one
     * @param unknown $param
     * @return string
     */
    function session_regenerate_id ($param) {
        ;
        return "";
    }
    /**
     *关闭会话
     *Session shutdown function
     * @param unknown $param
     * @return string
     */
    function session_register_shutdown ($param) {
        ;
        return "";
    }
    /**
     *Register one or more global variables with the current session
     *Register one or more global variables with the current session 
     * @param unknown $param
     * @return string
     */
    function session_register ($param) {
        ;
        return "";
    }
    /**
     *Re-initialize session array with original values
     * Re-initialize session array with original values
     * @param unknown $param
     * @return string
     */
    function session_reset ($param) {
        ;
        return "";
    }
    /**
     * 读取/设置当前会话的保存路径
     * Get and/or set the current session save path
     * @param unknown $param
     * @return string
     */
    function session_save_path($param) {
        ;
        return "";
    }
    /**
     * 设置会话 cookie 参数
     * Set the session cookie parameters
     * @param unknown $param
     * @return string
     */
    function session_set_cookie_params ($param) {
        ;
        return "";
    }
    /**
     *设置用户自定义会话存储函数
     *Sets user-level session storage functions
     * @param unknown $param
     * @return string
     */
    function session_set_save_handler ($param) {
        ;
        return "";
    }
    /**
     *启动新会话或者重用现有会话
     *Start new or resume existing session
     * @param unknown $param
     * @return string
     */
    function session_start($param) {
        ;
        return "";
    }
    /**
     *返回当前会话状态
     * Returns the current session status
     * @param unknown $param
     * @return string
     */
    function session_status ($param) {
        ;
        return "";
    }
    /**
     *Unregister a global variable from the current session
     *Unregister a global variable from the current session
     * @param unknown $param
     * @return string
     */
    function session_unregister($param) {
        ;
        return "";
    }
    /**
     *释放所有的会话变量
     *Free all session variables
     *Free all session variables
     * @param unknown $param
     * @return string
     */
    function session_unset($param) {
        ;
        return "";
    }
    /**
     *Write session data and end session
     * Write session data and end session
     * @param unknown $param
     * @return string
     */
    function session_write_close($param) {
        ;
        return "";
    }
}
